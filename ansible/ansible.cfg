[defaults]
inventory = inventory.ini
remote_user = ubuntu
private_key_file = /home/pfa/.ssh/key-devops.pem
host_key_checking = False
timeout = 60
interpreter_python = /usr/bin/python3

[ssh_connection]
ssh_args = -o IdentitiesOnly=yes -o StrictHostKeyChecking=no -o ConnectTimeout=60
